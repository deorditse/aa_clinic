name: aa_clinic
description: new app AAClinic
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'
#  sdk: '>=2.19.0-146.2.beta <3.0.0'
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  #добавляю слои
  business_layout:
    path: lib/packages/business_layout
  model:
    path: lib/packages/model
  style_app:
    path: lib/packages/style_app
  socket_io_client: ^2.0.0

  #  dio: ^4.0.6
  http: ^0.13.5
  font_awesome_flutter: ^10.1.0
  get: ^4.6.5
  table_calendar: ^3.0.7
  shared_preferences: ^2.0.12
  hive_flutter: ^1.1.0
  flutter_native_splash: ^2.1.6 #для экрана заставки
  flutter_datetime_picker: ^1.5.1
  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.5
  #  animations: ^2.0.5
  percent_indicator: ^4.0.1
  persistent_bottom_nav_bar: ^5.0.2
  intl: ^0.17.0 #для перевода текста / локаль / DateFormat
  dotted_border: ^2.0.0+2
  mask_text_input_formatter: ^2.4.0
  shimmer: ^2.0.0
  photo_view: ^0.14.0

  image_picker: ^0.8.6
  health: ^4.3.0
  #  socket_io_client: ^2.0.0
  permission_handler: ^10.2.0
  open_filex: ^4.3.2
  flutter_document_picker: ^5.1.0
  path_provider: ^2.0.11
  flutter_local_notifications: ^12.0.4
  timezone: ^0.9.0
  flutter_native_timezone: ^2.0.0
  flutter_markdown: ^0.6.13
dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/icons/menu_icons/
    - assets/icons/chats/
    - assets/icons/
    - assets/images/
    - assets/icons/menu_icons_otherPage/
    - assets/icons/menu_icons_otherPage/for_light_theme/
    - assets/achievement_delete_after_test/
    - assets/images/light_theme/
    - assets/icons/for_light_theme/

  fonts:
    - family: Oswald
      fonts:
        - asset: assets/fonts/Oswald/Oswald-VariableFont_wght.ttf
        - asset: assets/fonts/Oswald/Oswald-Medium.ttf
          weight: 500
    #
    - family: Ubuntu
      fonts:
        - asset: assets/fonts/Ubuntu/Ubuntu-Regular.ttf
        - asset: assets/fonts/Ubuntu/Ubuntu-Medium.ttf
        - asset: assets/fonts/Ubuntu/Ubuntu-Light.ttf
          weight: 400

